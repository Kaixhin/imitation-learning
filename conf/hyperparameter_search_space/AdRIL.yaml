# @package hydra.sweeper.ax_config

defaults:
  - SAC

params:
  # TODO: BC pretraining?
  imitation.absorbing:
    type: choice
    values: [true, false]
    value_type: bool
  # TODO: BC aux loss?
  imitation.balanced:
    type: choice
    values: [true, false]
    value_type: bool
  imitation.update_freq:
    type: choice
    values: [0, 1250, 2500, 5000, 12500, 25000, 50000]
    value_type: int
