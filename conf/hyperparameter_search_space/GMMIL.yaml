# @package hydra.sweeper.ax_config

defaults:
  - SAC

params:
  # TODO: BC pretraining?
  imitation.absorbing:
    type: choice
    values: [true, false]
    value_type: bool
  imitation.mix_expert_data:
    type: choice
    values: [true, false]
    value_type: bool
  # TODO: BC aux loss?
